# Agent guidance

Keep changes aligned with `doc/lab1/API.md` and `REQUIREMENTS.md`. Preserve SQLite persistence, JSON error
envelopes, and the decisions recorded in `DECISIONS.md`. Run the smallest relevant syntax or HTTP checks before
reporting completion. Do not add credentials, host-path mounts, or unrelated refactors.
